# Copy to .env and adjust as needed. The app runs fine without any of these.

# SQLite database location (default: backend/second_arrow.db)
# DATABASE_URL=sqlite:///./second_arrow.db

# Comma-separated list of allowed CORS origins for the frontend dev server.
# CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
