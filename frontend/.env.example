# Copy to .env to override defaults. None of these are required for local dev,
# because the Vite dev server proxies /api to the backend automatically.

# Use an absolute API base instead of the dev proxy (e.g. for a hosted backend).
# VITE_API_BASE=http://localhost:8000
